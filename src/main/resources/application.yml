spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://temp-data-instance.ch72rutnyb4j.ap-northeast-2.rds.amazonaws.com:3306/foodtruck
    username: admin
    password: ${password}
  jpa:
    properties:
      hibernate:
        format_sql: 'true'
        use_sql_comments: 'true'
        show_sql: 'true'
    hibernate:
      ddl-auto: update
  profiles:
    include: KEY
  data:
    redis:
      host: localhost
      port: 6379
      repositories:
        enabled: false